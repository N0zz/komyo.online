.DS_Store
*.swp
*~

# Generated marketing score-card PNG renders (regenerate: node scripts/render-scorecards.mjs)
plans/scorecards/

# Portal (itch.io / Newgrounds) zip builds (regenerate: node scripts/package-game.mjs <slug>)
dist-portal/

# Whole-site offline bundle (built at deploy + regenerate: node scripts/package-site.mjs)
dist-site/
komyo.zip

# Playwright MCP output — a11y snapshots, console logs, QA/eyeball-pass screenshots
# (save screenshots into this dir; never *.png at root — game icons/og-image are tracked)
.playwright-mcp/
node_modules/
